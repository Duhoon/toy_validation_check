/* 로그인 페이지 유효성 검사*/


let signinIdCheck = (id)=>{
    return id.length > 0 ? true : false;
}

let signinPasswordCheck = (password)=>{
    return password.length > 0 ? true : false;
}

/* 회원가입 페이지 유효성 검사 */

let signupIdCheck = (id)=>{
    return 
}